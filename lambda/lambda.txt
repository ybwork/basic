Применяется как правило для объявления анонимных функций по месту их использования.

Лямбда функции это функции без имени lambda x: return x + 1

Не рекомендуется использовать функцию map вместе с Лямбда.

Замыкание — это функция, охватывающая или замыкающая текущую область видимости.